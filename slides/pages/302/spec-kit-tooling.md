---
layout: default
section: Spec Kit
---

# Spec Kit in Practice

<img src="/302/spec-kit.png" class="mx-auto rounded shadow-lg" style="max-height: 440px;" />

<div class="mt-4 text-center text-sm opacity-70">

Spec Kit installs prompt files into your repository. Each prompt enforces one step of the workflow — making the discipline repeatable across teams.

</div>

<!--
Spec Kit is not a heavy methodology. It's a set of prompt files that encode the SDD workflow:

- /speckit.constitution → define non-negotiables
- /speckit.specify → describe what and why
- /speckit.clarify → find gaps in the spec
- /speckit.plan → technical approach
- /speckit.tasks → break into implementable units

The prompt files integrate directly with Copilot Chat.
The value: it enforces order (think first, build second) through tooling, not willpower.

Teams can customize each prompt to match their conventions, architecture, and quality standards.
-->
