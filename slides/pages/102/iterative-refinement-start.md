---
layout: default
section: Iterative Refinement
---

# Iterative Refinement: Start Simple

### First prompt - get something working

<img src="/102/fibonacci-first.png" class="w-full object-contain max-h-72 rounded shadow" />

<div class="mt-3 text-sm opacity-70">The first response works - but uses recursion. Let’s refine.</div>

<!--
First step in the three-slide iterative refinement sequence.

The Fibonacci example is simple enough that attendees focus on the PROCESS,
not the problem domain.

The first prompt is intentionally basic: "create a function to calculate Fibonacci numbers."
The response works but uses recursion — not what we want.

Key message: the first response is a starting point, not a final answer.
Don't rewrite the prompt — refine it.

The overall loop is: Draft → Evaluate → Refine → Validate.
One iteration is often enough. More than three means the context needs
restructuring, not more prompting.

Transition: "It works, but uses recursion. Let's add a constraint."
-->
